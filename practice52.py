# pip install

# from bs4 import BeautifulSoup
# soup = BeautifulSoup("<p>Some<b>bad<i>HTML")
# print(soup.prettify())


# 1. pypi.org 에서 pip 패키지들을 다운받을 수 있다.
# 2. 원하는 pip 패키지에 들어가서 설치하는 명령어를 복사, 적는다
# 3. vscode 설치 명령어를 터미널창에 적는다
# 4. 설치가 되었는지 확인한다. 상단에 주석처리된 코드가 있다.

''' pip 명령어는 다음과 같다
    install             패키지 설치         pip install [패키지]
    
    install --upgrade   패키지업그레이드    pip install --upgrade [패키지]
    
    uninstall           패키지 삭제         pip uninstall [패키지]
    
    list                설치 패키지 목록     pip list
    
    show                패키지 상세 정보     pip show[패키지]'''